# Day-16-Shell-Scripting-Basics

A Shell Script is a file that contains Linux commands written in sequence to automate tasks It usually runs using Bash, which interprets and executes the commands Basic concepts include shebang (#!/bin/bash), variables, user input (read), conditions (if-else), and simple loops Shell scripting is widely used in DevOps for automation like file handling, service management, and system monitoring.
